assistants:
  news_analyst:
    llm_deployment_name: ${NEWS_ANALYST_MODEL_NAME}
    bing_search_endpoint: "https://api.bing.microsoft.com/v7.0/news/search"
    max_news: 50
  financial_analyst:
    llm_deployment_name: ${FINANCIAL_ANALYST_MODEL_NAME}
  structured_report_generator:
    llm_deployment_name: ${REPORT_GENERATOR_MODEL_NAME}
    aoai_api_version: "2024-08-01-preview"
    output_folder: "./data/outputs"

financial_health_analysis:
  auth_provider_endpoint: "https://cognitiveservices.azure.com"
  key_vault_url: ${KEY_VAULT_URL}
